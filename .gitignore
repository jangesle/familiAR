# .luma files are tracked — GitHub Pages serves them directly.
# GitHub's per-file limit is 100 MB; files 50–100 MB get a warning but still push.
# If your captures exceed 100 MB, host them externally and use a full URL in source.

# macOS
.DS_Store
